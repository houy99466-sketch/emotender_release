# EmoTender「此刻一杯」技术说明文档 & 路演 Q&A

---

## 一、技术架构总览

```
┌─────────────────────────────────────────────────────┐
│                    用户输入层                         │
│  文字输入 / 语音输入（MiMo ASR）/ 表情选择            │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│              Layer 1: 情绪量化引擎                    │
│  LLM 语义分析 → NRC 八类情绪 + VAD 三维连续向量       │
│  输出: Universal Emotion Vector (UEV)                │
│  包含: 置信度 + 证据链 + 情绪趋势                     │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│              Layer 2: 情绪映射引擎                    │
│  情绪向量 → 评估维度 → 需求向量                       │
│  风味六维向量: 甜度/茶感/奶香/果香/清爽度/口感层次     │
│  余弦相似度匹配 → Top N 候选饮品                      │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│              Layer 3: 领域适配器                      │
│  饮品适配器: 38 杯喜茶饮品数据库                      │
│  空间适配器: 空调/灯光/净化器方案                     │
│  LLM 从候选中选择 + 生成个性化推荐理由                │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│                    输出层                             │
│  前端展示: 像素风对话 + 六维风味图 + 牛皮纸小票        │
│  机器人控制: 表情/动作/台词 JSON                      │
│  TTS 语音播报                                        │
└─────────────────────────────────────────────────────┘
```

---

## 二、核心技术模块详解

### 2.1 情绪量化引擎

**NRC Emotion Lexicon 八类情绪**

基于 Mohammad & Turney (2013) 的 NRC 情绪词典，将所有情绪归类为八种基础情绪：

| 英文键 | 中文 | 典型触发场景 |
|--------|------|-------------|
| anger | 愤怒 | 被插队、被甲方改了第八版 |
| anticipation | 期待 | 明天要答辩、等结果 |
| disgust | 厌恶 | 闻到奇怪的味道 |
| fear | 恐惧/焦虑 | 明天就要答辩了，脑子停不下来 |
| joy | 喜悦 | 今天面试过了！ |
| sadness | 悲伤 | 今天考试没考好 |
| surprise | 惊讶 | 没想到会这样 |
| trust | 信任 | 默认中性状态 |

**VAD 三维连续向量**

基于 Russell (1980) 环形模型 + Mehrabian & Russell (1974) PAD 模型：

```json
{
  "valence": -0.45,    // 愉悦度：负面 ← → 正面
  "arousal": 0.72,     // 唤醒度：平静 ← → 激动
  "dominance": 0.30,   // 支配度：失控 ← → 可控
  "intensity": 0.68    // 强度：微弱 ← → 强烈
}
```

**证据链机制**

每一条情绪判断都必须引用用户本轮对话中的原话：

```json
{
  "evidence": [
    {
      "quote": "明天就要答辩了，脑子停不下来",
      "emotions": ["fear", "anticipation"],
      "interpretation": "用户提到答辩和脑子停不下来，显示高唤醒、低控制感"
    }
  ]
}
```

**情绪趋势追踪**

系统记录最近 5 轮情绪变化，判断趋势：
- `escalating`（上升）→ 主动关怀："我注意到你最近几轮状态越来越紧张了"
- `steady`（稳定）→ 正常推荐流程
- `easing`（缓解）→ 正向反馈："感觉好些了，要试试新搭配吗？"

### 2.2 情绪→饮品匹配算法

**两层匹配机制**

```
用户情绪 → 情绪标签 → 适配分类（硬编码）
                     → 风味向量 → 余弦相似度排序（算法）
                     → LLM 从 Top N 选择 + 写推荐理由（生成式）
```

**Step 1: 情绪→分类筛选**

每种情绪标签映射到 3-4 个饮品分类：

| 情绪 | 适配分类 |
|------|---------|
| 焦虑 | 经典牛乳鲜奶茶、纯原叶无添加纯茶、抹茶&可可浓醇系列、限定工艺系列 |
| 难过 | 经典牛乳鲜奶茶、抹茶&可可浓醇系列、生打椰椰系列、时令季节限定 |
| 兴奋 | 招牌芝芝鲜果茶、生打椰椰系列、时令季节限定、限定工艺系列 |
| 疲惫 | 生打椰椰系列、纯原叶无添加纯茶、经典牛乳鲜奶茶、限定工艺系列 |
| 清醒 | 纯原叶无添加纯茶、喜乐芝纯茶奶盖、招牌芝芝鲜果茶、限定工艺系列 |
| 犹豫 | 招牌芝芝鲜果茶、经典牛乳鲜奶茶、喜乐芝纯茶奶盖、时令季节限定 |

**Step 2: 六维风味向量余弦相似度**

每杯饮品标注六维风味向量（0-100）：

```
(甜度, 茶感, 奶香, 果香, 清爽度, 口感层次)
```

示例：
| 饮品 | 甜度 | 茶感 | 奶香 | 果香 | 清爽度 | 口感层次 |
|------|------|------|------|------|--------|---------|
| 多肉葡萄 | 45 | 40 | 10 | 85 | 80 | 60 |
| 烤黑糖波波牛乳 | 75 | 30 | 85 | 10 | 20 | 75 |
| 碎银子普洱 | 12 | 88 | 5 | 3 | 35 | 58 |

情绪→目标风味的映射矩阵：

```python
FLAVOR_MATRIX = {
    "fear":     (55, 35, 65, 30, 45, 55),  # 焦虑 → 温热、奶香、不刺激
    "sadness":  (70, 25, 80, 30, 35, 65),  # 难过 → 更甜、更奶、更温暖
    "joy":      (65, 30, 35, 80, 75, 70),  # 开心 → 果香、清爽、明亮
    "anger":    (20, 70, 20, 30, 80, 65),  # 愤怒 → 茶感强、清爽、解腻
}
```

混合情绪时加权计算：

```python
target = sum(flavor[i] * score for emotion, score in scores.items())
         / sum(scores.values())
```

余弦相似度排序，返回 Top 10 候选。

**Step 3: LLM 最终选择**

从 Top 10 候选中，LLM 根据用户原话的具体语境选择最合适的 1 杯，并生成个性化推荐理由。

### 2.3 长期用户记忆

**用户档案结构**

```json
{
  "username": "demo",
  "taste_preferences": ["喜欢温热、奶香重的"],
  "avoidances": ["不喝咖啡因"],
  "drink_history": ["暖绒厚乳", "烤黑糖波波牛乳"],
  "emotion_patterns": ["低落时倾向选择温暖甜味的食物"],
  "conversation_style": "话少型"
}
```

**记忆机制**

- Login 时加载档案
- Logout 时将本次会话压缩写入长期档案
- 档案只影响口味偏好、避忌、交流风格
- **情绪判断严格隔离**：本轮情绪只根据本轮原话判断，不注入历史情绪

### 2.4 空间氛围方案

基于 NRC 主次情绪，生成空调/灯光/净化器建议：

| 情绪 | 空调温度 | 灯光亮度 | 灯光色温 | 净化器 |
|------|---------|---------|---------|--------|
| 焦虑 | 25°C | 40% | 3000K 暖光 | 静音 |
| 兴奋 | 24°C | 80% | 4200K 自然光 | 自动 |
| 难过 | 25°C | 45% | 3000K 暖光 | 静音 |
| 愤怒 | 23°C | 45% | 3500K 中性光 | 自动 |

主次情绪混合时按比例插值（次情绪权重上限 35%）。

---

## 三、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端 | FastAPI + Python 3.10 | RESTful API 服务 |
| LLM | MiMo (api.xiaomimimo.com) | 情绪分析 + 推荐理由生成 |
| ASR | MiMo ASR | 语音转文字 |
| TTS | MiMo TTS | 文字转语音 |
| 前端 | HTML/CSS/JS + 像素风场景 | 单页应用，无框架 |
| 情绪引擎 | NRC Emotion Lexicon | 八类情绪 + VAD 向量 |
| 匹配算法 | 余弦相似度 | 六维风味向量匹配 |
| 数据库 | JSON 文件 | 用户档案本地存储 |

---

## 四、路演 Q&A 预案

### Q1：这和现有的情感分析有什么区别？

**A：** 现有情感分析输出的是"正面/负面"或者"开心/难过"一个标签。我们输出的是一个 **三维连续向量 + 八类标签 + 置信度 + 证据链 + 趋势**。

标签适合做展示，向量适合做计算。我们两个都给——面向人的用标签，面向机器的用向量。

而且每一层情绪判断都必须引用用户本轮对话里的原话作为证据。系统不能凭空说"你焦虑"，它必须说："你说'明天就要答辩了，脑子停不下来'——这句话对应的唤醒度是 0.72，主导感是 0.30。"

---

### Q2：VAD 向量的准确性怎么保证？

**A：** 我们不追求"绝对准确"，因为情绪本身就没有客观真值。我们追求的是 **有证据、可解释、能校准**。

每一层输出都附带置信度和证据引用，用户可以反馈"分析不准"，系统会调整。

Russell 的环形模型、NRC 词典、Scherer 的评估理论——这三个框架交叉验证。

---

### Q3：你们是怎么从情绪映射到具体饮品的？

**A：** 三层机制：

1. **硬编码分类筛选**：每种情绪标签映射到 3-4 个饮品分类（如焦虑→牛乳茶、纯茶），缩小范围
2. **六维风味向量余弦相似度**：每杯饮品标注甜度/茶感/奶香/果香/清爽度/口感层次六个维度，情绪也有对应的目标风味向量，用余弦相似度排序
3. **LLM 最终选择**：从 Top 10 候选中，LLM 根据用户原话的具体语境选择最合适的 1 杯，并写个性化推荐理由

不是简单的查表，是 **规则筛选 + 向量匹配 + 生成式推荐** 的三层结合。

---

### Q4：为什么用余弦相似度而不是其他算法？

**A：** 余弦相似度衡量的是两个向量的"方向相似性"，而不是绝对距离。这正好符合我们的需求：

- 用户说"焦虑"，目标风味是 (55, 35, 65, 30, 45, 55)
- 烤黑糖波波牛乳是 (75, 30, 85, 10, 20, 75)
- 虽然绝对值不同，但方向一致（都是"高甜、高奶香、低果香"），相似度很高

如果用欧氏距离，高维度的向量会被某个极端维度主导，不如余弦相似度稳定。

---

### Q5：混合情绪怎么处理？

**A：** 混合情绪是我们的核心优势之一。

用户说"今天答辩被怼了，但过了"，系统会输出：
- fear: 0.4, joy: 0.35, anticipation: 0.25

然后加权计算目标风味向量：

```python
target = (fear风味 × 0.4 + joy风味 × 0.35 + anticipation风味 × 0.25) / 1.0
```

这样得到的风味向量是三种情绪的"中间地带"，匹配到的饮品既能安抚焦虑，又能庆祝通过。

---

### Q6：长期记忆是怎么工作的？会不会泄露隐私？

**A：**

**记忆内容**：只存稳定偏好（口味、避忌、交流风格、历史饮品），不存原始对话。

**情绪隔离**：本轮情绪严格只根据本轮原话判断，不注入历史情绪。这是为了防止系统"贴标签"——用户今天焦虑，不代表明天也焦虑。

**隐私保护**：
- 数据存储在本地 JSON 文件，不上传云端
- 用户可以随时查看、修改、删除
- Logout 时压缩写入，原始对话不保留

---

### Q7：如果用户说的话很模糊，系统怎么处理？

**A：** 系统有三层容错：

1. **置信度机制**：当证据不足时，confidence 降低，clarification_needed 设为 true
2. **追问机制**：茗茗会问一个问题（最多一轮），如果还模糊就直接帮用户决定
3. **兜底机制**：如果 LLM 调用失败，系统会用 fallback 方案（绿妍，清爽低甜的冷启动饮品）

---

### Q8：你们的38杯饮品是怎么来的？

**A：** 来自喜茶（HEYTEA）的真实菜单，覆盖 8 个系列：

| 系列 | 数量 | 代表饮品 |
|------|------|---------|
| 招牌芝芝鲜果茶 | 7 | 多肉葡萄、芝芝莓莓 |
| 生打椰椰系列 | 4 | 生打椰椰奶冻、多肉芒芒甘露 |
| 经典牛乳鲜奶茶 | 4 | 烤黑糖波波牛乳、咸酪碎银子 |
| 喜乐芝纯茶奶盖 | 4 | 芝士绿妍、芝士金凤茶王 |
| 抹茶&可可浓醇系列 | 3 | 三倍厚抹茶拿铁、苦巧咸酪 |
| 纯原叶无添加纯茶 | 5 | 绿妍、碎银子普洱 |
| 限定工艺系列 | 3 | 铜锅现煲锅煲乳茶 |
| 时令季节限定 | 8 | 荔枝系列、车厘子鲜果茶 |

每杯都标注了六维风味向量，确保情绪匹配覆盖所有饮品。

---

### Q9：系统的响应速度怎么样？

**A：** 单轮对话约 30-60 秒，其中：
- LLM 情绪分析：10-20 秒
- 饮品匹配（余弦相似度）：< 1 毫秒
- LLM 推荐理由生成：10-20 秒
- TTS 语音合成：5-10 秒

瓶颈在 LLM 调用。饮品匹配是纯算法，几乎不耗时。

---

### Q10：这个系统的商业化路径是什么？

**A：** **情绪量化即服务（EQaaS）**。

- 核心 API 对外开放：`/v2/emotion/quantify` 输出通用情绪向量
- `/v2/emotion/adapt?domain=` 路由到各领域适配器
- 垂直场景落地：餐饮、音乐、空间、消费
- 小米生态天然适配空间适配器（米家智能家居）

今天是茶饮，明天可以是音乐、是活动、是空间——同一个情绪信号，驱动所有领域。

---

### Q11：和 ChatGPT 直接问"我该喝什么"有什么区别？

**A：** 三个核心差异：

1. **结构化输出**：ChatGPT 给你一段文字建议，我们给你一个可计算的情绪向量，可以对接任何下游系统
2. **证据链**：我们的每条情绪判断都有原话引用，可追溯、可验证
3. **领域适配**：ChatGPT 不知道喜茶有什么饮品，不知道你的口味偏好，不知道你上次喝了什么。我们的系统有 38 杯真实饮品的风味向量 + 用户长期档案

---

### Q12：如果 LLM 返回了错误的情绪判断怎么办？

**A：** 多层防护：

1. **NRC 验证**：检查八类情绪键名、分数范围（0-1）、总和（≈1.0）、主情绪是否匹配最高分
2. **证据验证**：每条 evidence 的 quote 必须存在于当前会话中，每个非零情绪都必须有证据
3. **中英文兼容**：LLM 可能返回中文情绪名（如"喜悦"），系统自动映射到英文键（"joy"）
4. **重试机制**：验证失败自动重试，最多 3 次，指数退避
5. **Fallback 兜底**：3 次都失败则使用 fallback 方案

---

### Q13：前端的像素风场景是怎么做的？

**A：** 纯 CSS + JavaScript 实现：

- **场景状态机**：empty → entering（入场动画）→ chatting → serving（递饮品动画）→ recommendation_ready
- **气泡对话**：CSS clip-path 实现像素风格气泡，支持拖拽定位
- **表情系统**：SVG 绘制的 6 种情绪表情（清醒、兴奋、难过、疲惫、焦虑、犹豫），带呼吸灯动画
- **视频动画**：customer-enter.mp4（顾客入场）、mingming-serve.mp4（茗茗递饮品）

---

### Q14：空间氛围方案是怎么生成的？

**A：** 基于 NRC 主次情绪的硬编码预设 + 比例插值：

- 每种情绪有对应的空调温度、灯光亮度/色温、净化器模式
- 主次情绪混合时，次情绪权重上限 35%，按比例插值
- 空调限制 16-28°C，灯光限制 20-100% / 2700-6500K
- 只做预览，不实际控制设备

---

### Q15：系统的扩展性怎么样？加新品类需要改多少？

**A：** 架构设计上是三层解耦：

1. **加新饮品**：只需要在 `drink_matcher.py` 的 `DRINK_DB` 里加一行（名称、分类、六维向量、描述），不需要改其他代码
2. **加新领域**（如音乐适配器）：只需要新增一个领域适配器模块，不需要改情绪引擎和映射引擎
3. **换 LLM**：只需要改 `.env` 里的 API 地址和模型名，不需要改业务逻辑

---

### Q16：你们用了哪些心理学理论？

**A：**

| 理论 | 提出者 | 我们怎么用的 |
|------|--------|-------------|
| 环形模型 | Russell (1980) | VAD 二维/三维情绪空间 |
| PAD 三维模型 | Mehrabian & Russell (1974) | valence + arousal + dominance |
| NRC 情绪词典 | Mohammad & Turney (2013) | 八类情绪标签 |
| 评估倾向理论 | Lerner & Keltner (2000) | 同一效价不同情绪→不同决策倾向 |
| 情绪调节理论 | Gross (1998) | 用户长期档案中的调节风格 |
| 拓展-建构理论 | Fredrickson (2001) | 正向情绪放大功能 |
| 情绪惯性 | Kuppens et al. (2010) | 情绪趋势追踪 |

---

### Q17：如果用户说脏话或者极端言论怎么办？

**A：** 系统有安全协议触发词：

```python
SAFETY_TRIGGERS = ("未成年", "喝醉", "开车", "酒驾", "吃药", 
                   "失眠怎么治", "抑郁诊断", "自杀", "伤害别人")
```

触发后进入 safety 模式：
- 只推荐无咖啡因温和饮品
- 不做心理诊断
- 茗茗会说："有些话我不够资格接。但可以陪你喝一杯。"

---

### Q18：系统的测试覆盖情况？

**A：** 

- **单元测试**：情绪验证、用户档案、对话模式、前端流程
- **集成测试**：API 端点、LLM 调用、饮品匹配
- **验证测试**：NRC 中英文验证、38 杯饮品全覆盖、混合情绪、边界情况
- **视觉测试**：截图对比、像素风场景渲染

---

### Q19：为什么选择喜茶而不是其他品牌？

**A：** 三个原因：

1. **菜单丰富**：38 杯饮品覆盖 8 个系列，从鲜果茶到纯茶到奶茶，品类齐全
2. **标准化**：喜茶的饮品配方标准化程度高，风味特征明确
3. **受众匹配**：喜茶的用户群体（年轻人、白领）和我们的情绪茶饮场景高度重合

---

### Q20：这个项目花了多长时间？

**A：** 从零到 Demo 大约 2 周：

- Week 1：后端架构 + LLM prompt 调试 + 情绪引擎 + 饮品数据库
- Week 2：前端像素风场景 + 长期记忆 + 空间方案 + 测试 + 路演准备

核心难点不在代码量，在于 prompt engineering——让 LLM 稳定输出结构化的 JSON，并且情绪判断准确。
